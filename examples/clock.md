# Time Server Specification

A minimal clock app that displays the current time to the second.

## Design
- Clock is centered on the page with a clean sans-serif font
- The clock has a title above it that is passed into the environment as APP_TITLE, if null display "CLOCK"
- Each digit has a slightly different rotation that changes on each tick
